@model TMNT.Models.ViewModels.IntermediateStandardViewModel

@{
    ViewBag.Title = "Edit";
}

@section AddToHead {
    @Styles.Render("~/maxxam-forms")
}

<div class="row">
    <div class="col-lg-12">
        <h1 class="page-header">
            Edit @Model.IdCode
        </h1>
        <ol class="breadcrumb">
            <li class="active">
                <i class="fa fa-dashboard"></i>@Html.ActionLink(" Dashboard", "Index", "Home") / <i class="fa fa-flask"></i> Stock Standards / <i class="fa fa-clipboard"></i> Edit @Model.IdCode
            </li>
        </ol>
    </div>
</div>

<p>
    @Html.ActionLink("Back to List", "Index", "IntermediateStandard", null, htmlAttributes: new { @class = "btn btn-default" })
    @Html.ActionLink("View " + Model.IdCode + " Details", "Details", new { id = Model.IntermediateStandardId }, htmlAttributes: new { @class = "btn btn-default" })
</p>

@using (Html.BeginForm("Edit", "IntermediateStandard", null, FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <hr />
        @Html.ValidationSummary(true)
        @Html.HiddenFor(model => model.IntermediateStandardId)

        @*<div class="form-group">
            @Html.LabelFor(model => model.Amount, new { @class = "control-label col-md-3" })
            <div class="col-md-4">
                @Html.EditorFor(model => model.Amount, new { htmlAttributes = new { @class = "input-summary form-control no-border-radius", @min = "0" } })
                @Html.ValidationMessageFor(model => model.Amount)
            </div>
        </div>*@

        <div class="form-group">
            @Html.LabelFor(model => model.Replaces, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.Replaces)
                @Html.ValidationMessageFor(model => model.Replaces)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.ReplacedBy, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.ReplacedBy)
                @Html.ValidationMessageFor(model => model.ReplacedBy)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}